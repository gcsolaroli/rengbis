# Expected: fully resolved schema for multi-import.rengbis

= {
    person: {
        name: text,
        email: text { regex = "^[\w\-\.]+@([\w-]+\.)+[\w-]{2,}$" }
    },
    credentials: {
        user: text { 3 <= length <= 20 },
        pass: text { 8 <= length }
    }
}
